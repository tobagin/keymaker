using Gtk 4.0;
using Adw 1;

template $KeyMakerEmptyStateRow : Adw.ActionRow {
  title: _("No Keys Found");
  subtitle: _("Deploy a key to get started");
  icon-name: "dialog-information-symbolic";
}
